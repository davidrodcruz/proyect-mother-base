{
  "buildCommand": "cd apps/frontend && npx astro build",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "pnpm install",
  "framework": "astro",
  "rewrites": [
    {
      "source": "/api/v1/(.*)",
      "destination": "/api/v1/[[path]].ts"
    }
  ]
}
